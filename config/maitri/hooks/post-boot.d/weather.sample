#!/bin/bash

# Show the weather status notification after maitri has started.
# To put it into use, remove .sample from this file name.

weather=$(maitri-weather-status 2>/dev/null) || true

if [[ -n $weather && $weather != "Weather unavailable" ]]; then
  maitri-notification-send -u low "$weather"
fi
