#!/bin/bash

set -e

if maitri-done ensure voxtype-install-invitation; then
  maitri-notification-send -u critical -g  "Install Dictation with Voxtype" \
    "Click to install voice dictation for maitri." \
    --exec maitri-launch-floating-terminal-with-presentation maitri-voxtype-install
fi
