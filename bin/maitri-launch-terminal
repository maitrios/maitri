#!/bin/bash

# maitri:summary=Launch a terminal in the active terminal's current directory
# maitri:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(maitri-cmd-terminal-cwd)" "$@"
