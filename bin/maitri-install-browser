#!/bin/bash

# maitri:summary=Install a supported browser
# maitri:args=<chromium|chrome|brave|brave-origin|edge|firefox|zen>
# maitri:examples=maitri install browser firefox | maitri install browser brave

source "$MAITRI_PATH/install/helpers/browser-policy.sh"

announce_browser_installed() {
  echo ""
  echo "$1 browser installed. Make it the default via Setup > Defaults > Browser."
}

copy_chromium_flags() {
  mkdir -p ~/.config
  cp -f "$MAITRI_PATH/config/chromium-flags.conf" "$1"
  maitri-install-chromium-copy-url
  maitri-install-chromium-ytdlp
}

setup_firefox_wayland() {
  mkdir -p ~/.config/environment.d
  echo "MOZ_ENABLE_WAYLAND=1" > ~/.config/environment.d/maitri-firefox-wayland.conf
}

case $1 in
chromium)
  echo "Installing Chromium..."
  maitri-pkg-add chromium || exit 1

  copy_chromium_flags ~/.config/chromium-flags.conf
  announce_browser_installed "Chromium"
  ;;
chrome)
  echo "Installing Chrome..."
  maitri-pkg-aur-add google-chrome || exit 1

  copy_chromium_flags ~/.config/chrome-flags.conf
  announce_browser_installed "Chrome"
  ;;
edge)
  echo "Installing Edge..."
  maitri-pkg-aur-add microsoft-edge-stable-bin || exit 1

  copy_chromium_flags ~/.config/microsoft-edge-stable-flags.conf
  announce_browser_installed "Edge"
  ;;
brave)
  echo "Installing Brave..."
  maitri-pkg-aur-add brave-bin || exit 1

  copy_chromium_flags ~/.config/brave-flags.conf
  announce_browser_installed "Brave"
  ;;
brave-origin)
  echo "Installing Brave Origin..."
  maitri-pkg-aur-add brave-origin-bin || exit 1

  copy_chromium_flags ~/.config/brave-origin-flags.conf
  announce_browser_installed "Brave Origin"
  ;;
firefox)
  echo "Installing Firefox..."
  maitri-pkg-add firefox || exit 1

  browser_policy_setup_firefox_distribution /usr/lib/firefox/distribution
  setup_firefox_wayland
  announce_browser_installed "Firefox"
  ;;
zen)
  echo "Installing Zen..."
  maitri-pkg-aur-add zen-browser-bin || exit 1

  browser_policy_setup_firefox_distribution /opt/zen-browser/distribution
  setup_firefox_wayland
  announce_browser_installed "Zen"
  ;;
*)
  echo "Usage: maitri-install-browser <chrome|brave|brave-origin|edge|firefox|zen>"
  exit 1
  ;;
esac
