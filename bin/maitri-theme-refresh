#!/bin/bash

# maitri:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/maitri/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  MAITRI_THEME_SKIP_BACKGROUND=1 maitri-theme-set "$(cat $THEME_NAME_PATH)"
fi
