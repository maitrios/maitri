#!/bin/bash

# maitri:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/maitri/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! maitri-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
