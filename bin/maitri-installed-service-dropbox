#!/bin/bash

# maitri:summary=Check whether Dropbox is installed and running
# maitri:hidden=true

set -euo pipefail

if maitri-cmd-present dropbox-cli && dropbox-cli running >/dev/null 2>&1; then
  exit 0
fi

pgrep -x dropbox >/dev/null 2>&1
