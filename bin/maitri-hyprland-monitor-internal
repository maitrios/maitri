#!/bin/bash

# maitri:summary=Enable, disable, toggle, or recover the internal laptop display
# maitri:args=<on|off|toggle|recover>

TOGGLE="internal-monitor-disable"
TOGGLE_FLAG="$HOME/.local/state/maitri/toggles/hypr/$TOGGLE.lua"
MIRROR_TOGGLE="internal-monitor-mirror"

INTERNAL=$(maitri-hyprland-monitor-laptop)

wake() {
  hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })' >/dev/null 2>&1 || true
}

on() {
  if maitri-hyprland-toggle-enabled $TOGGLE; then
    maitri-hyprland-toggle $TOGGLE off
    maitri-notification-send -g 󰍹 "Laptop display enabled"
  fi

  wake
}

off() {
  if [[ -z $INTERNAL ]]; then
    maitri-notification-send -g 󰍹 "No laptop display found"
    exit 1
  fi

  # The name is written into generated Lua below, so only a plain connector
  # name may pass; anything else could execute on the next reload.
  if [[ ! $INTERNAL =~ ^[A-Za-z0-9._-]+$ ]]; then
    maitri-notification-send -g 󰍹 "Refusing unsafe monitor name"
    exit 1
  fi

  if ! maitri-hyprland-monitor-external-active; then
    maitri-notification-send -g 󰍹 "Can't disable the only active display"
    exit 1
  fi

  if maitri-hyprland-toggle-disabled $TOGGLE && maitri-hyprland-toggle-disabled $MIRROR_TOGGLE; then
    printf 'hl.monitor({ output = "%s", disabled = true })\n' "$INTERNAL" >"$TOGGLE_FLAG"
    maitri-notification-send -g 󰍹 "Laptop display disabled"
    hyprctl reload
  fi
}

recover() {
  # Runs from the clamshell watcher every few seconds, so it must be a no-op
  # unless it actually re-enables a display: an unconditional wake here undoes
  # lock-screen blanking and races the resume modeset into a visible flash.
  maitri-hyprland-monitor-external-active && return 0
  maitri-hyprland-toggle-enabled $TOGGLE || return 0

  maitri-hyprland-toggle $TOGGLE off
  wake
}

toggle() {
  if maitri-hyprland-toggle-enabled $TOGGLE; then
    on
  else
    off
  fi
}

case "$1" in
  on) on ;;
  off) off ;;
  toggle) toggle ;;
  recover) recover ;;
  *)
    echo "Usage: $(basename "$0") {on|off|toggle|recover}" >&2
    exit 1
    ;;
esac
